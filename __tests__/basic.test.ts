// __tests__/basic.test.ts
describe("Basic test", () => {
  it("should pass", () => {
    expect(1 + 1).toBe(2);
  });

  it("should handle basic logic", () => {
    const result = 2 * 3;
    expect(result).toBe(6);
  });
});
